# Propagate selected annotations from fields to the generated
# RequiredArgsConstructor parameters so @Qualifier injection works.
lombok.copyableAnnotations += org.springframework.beans.factory.annotation.Qualifier
